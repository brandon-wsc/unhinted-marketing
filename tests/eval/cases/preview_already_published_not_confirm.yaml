id: preview_already_published_not_confirm
suites: [regression]
node: route_intent
state:
  mode: PREVIEW
  research_rule_pass: false
  draft:
    caption: 星期五 OT 完飲罐嘢先走。
  messages:
    - role: user
      content: 唔使啦，我頭先自己出咗
expect:
  parse_ok: true
  # "Already published" is information, not a publish request — never confirm.
  intent_not: confirm_intent
